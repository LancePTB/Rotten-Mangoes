<h1>Rotten Mangoes</h1>
<hr>
<%= paginate @users %>
<%= link_to "Create a user!", new_user_path %>
<hr>
<% @users.each do |user| %>
  <h2><%= user.firstname %> <%= user.lastname %></h2>
  (<%= link_to "Delete this user", user_path(user), method: :delete, confirm: "You sure?" %>)
  <hr>
<% end %>
